name: F-Nodes

on:
  workflow_dispatch:
#  schedule:
#    - cron: '0 18,0,6,12 * * *'

concurrency:
  group: mc-startup
  cancel-in-progress: true

jobs:
  renew:
    runs-on: ubuntu-latest
    timeout-minutes: 5  # 强制 5 分钟超时，防止挂死浪费额度
    permissions:
      contents: write
      actions: write
    concurrency:
      group: falix-renew
      cancel-in-progress: false

    steps:
      - uses: actions/checkout@v4

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装基础依赖
        run: |
          sudo apt-get update && sudo apt-get install -y xvfb unzip jq
          pip install seleniumbase>=4.25.0 pyvirtualdisplay>=3.0 requests>=2.31.0
          sbase install chromedriver

      - name: 启用 WARP 网络
        uses: fscarmen/warp-on-actions@v1.3
        with:
          stack: dual
          mode: client

      - name: 执行续期任务
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          FALIX_SERVER_IP: ${{ secrets.FALIX_SERVER_IP }}
        run: |
         # mkdir -p output/screenshots
          python main.py

      # 运行完毕后推送更新（如 main.py 修改或写出了 time.txt）
    #  - name: 保存仓库变更 (包含 time.txt)
    #    if: always()
    #    run: |
    #      git config --global user.name "github-actions[bot]"
    #      git config --global user.email "github-actions[bot]@users.noreply.github.com"
    #      git add time.txt || true
    #      git commit -m "update: sync time.txt [skip ci]" || echo "No changes to commit"
    #      git push origin main --force || true
